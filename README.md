# MultiModuleChatProject

A cross-platform Python project for text-to-voice chat with extensible modules.

## Setup
Run `python scripts/setup.py` for initial setup.

## Run
Use `python src/main.py` to start the application.